# Exercise 3 - Docker Containerization

This exercise containerizes the IoT sensor data generator.

## Build

docker build -t sensor-generator .

## Run

docker run --rm sensor-generator

## Docker Compose

docker compose up
